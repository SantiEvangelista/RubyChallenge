meta {
  name: Create Product (Async)
  type: http
  auth: bearer
}

post { url: http://localhost:8080/products?delay=10
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Example Product",
    "author": "{{username}}"
  }
}
