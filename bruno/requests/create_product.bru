meta {
  name: Create Product (Async)
  type: http
  auth: bearer
}

post { 
  url: {{base_url}}/products?delay=10
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Example Product",
    "author": "{{username}}"
  }
}
